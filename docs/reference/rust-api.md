# Rust API

The Rust API documentation is generated by `cargo doc` and embedded below. If the frame does not load, you may need to [build the docs locally](#building-locally).

<iframe src="../_rust_api/uvid/index.html" width="100%" height="800px" style="border: 1px solid var(--md-default-fg-color--lightest); border-radius: 4px;"></iframe>

## Building Locally

To generate and view the Rust API docs:

```bash
cargo doc --no-deps --open
```

In the CI pipeline, `cargo doc` output is copied to `docs/_rust_api/` so it is available in the deployed site. When developing locally, run the command above to browse the Rust docs directly.
